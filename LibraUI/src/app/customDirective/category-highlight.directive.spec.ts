import { CategoryHighlightDirective } from './category-highlight.directive';

describe('CategoryHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new CategoryHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
